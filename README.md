# Reinforce-Learning-Trajectory-Planning
通过强化学习对6轴机器人进行轨迹规划，包含以下文件
# ------------------------------------------------------
robot                   机器人，包含DHRobot和CollisionRobot，前者用于计算DH模型，解算最终位姿误差；后者用于碰撞建模，后续用作碰撞检测
environment             环境，包含Robot建模，用于模拟环境中机器人行动并将数据输出用于训练，后续会生成行动轨迹
agent                   智能体，包含PPOAgent和SACAgent，其中SACAgent尚未完成
train                   训练智能体，用于调用Agent中的智能体进行训练
read_and_write_file     文件读写，包含轨迹文件读写和智能体数据文件读写
visiualization          可视化，用于将生成的轨迹和姿态进行可视化，后续还要添加，尚未完成
main                    主函数，训练时跑这个函数
test                    写代码过程中写的一些测试代码，没什么用
subject                 一些独立的小代码，用于实现某些功能
# ------------------------------------------------------

# 版本迭代=====================================
--------------------------
版本 0.1.0    日期 26.3.27
主要实现功能：
实现了最基本的训练，采用的奖励函数、神经网络输入、神经网络组成均不完善，最终实现的效果可以运行，但是结果并不收敛
问题：
1. 神经网络、奖励函数、神经网络输入这三部分最需要修改，目标是使训练过程收敛
2. 文件读写部分尚未验证，后续需要测试
3. 可视化部分未完成，在完成上述问题的解决以后需要做一个动图的可视化，显示最终生成的轨迹的仿真结果       完成：20260402 by xlxlqqq ./utils/TraceVisiualization.py中的trace_trajectory_to_gif接口
4. 碰撞检测没有添加，可以在版本0.1.1完成后再做打算
5. 关节角度限制应该添加到惩罚函数中
--------------------------

--------------------------
版本 0.1.1    日期 26.3.30
更新内容：
1. 删除了origin_action，全部同一采用action
2. 更新了部分内容，但是没有实质性进展，代码依然无法收敛
--------------------------

--------------------------
版本 0.1.2    日期 26.4.2
更新内容：
1. 优化了网络输入，将网络中的角度误差输入删除
2. 修复了train中done_or_not的问题，之前的版本中一定会是True，现在根据success判断是否成功
3. 将每个episode的reward输出为一个txt文件，保存在log/data/reward/{date}目录下
4. 添加了最简单的学习率衰减，有待优化
--------------------------

--------------------------
版本 0.1.3    日期 26.4.9
更新内容：
1. 大角度随机位置，收敛至50度以内  **最重要**
2. 删除了environment中的action_bound，实际上这个功能在PPOAgent中已经实现了
3. 将每次运动限制范围写为结构体内参，默认为5.0
4. 修改了日志显示，现在日志显示更加美观
5. 重新整理数据输出文件夹，即log文件夹，将reward输出为一个txt文件，每次更新覆盖；将best.pt依据版本缓存；train中现版本所用的pt文件不上传
6. 重新设计了奖励，现在的奖励不以角度误差为基准，而是以是否更近为基准
7. 重新设计了lr，依据于上一次是否有大的loss变化
8. 添加了一个测试集，每一定次数的episode后显示在典型场景中的训练效果
--------------------------
# =============================================

